(()=>{const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;if(reduced)return;const root=document.documentElement;let tx=0,ty=0;addEventListener('pointermove',e=>{tx=(e.clientX/innerWidth-.5)*6;ty=(e.clientY/innerHeight-.5)*4;root.style.setProperty('--pointer-x',`${tx}px`);root.style.setProperty('--pointer-y',`${ty}px`)});document.querySelectorAll('button,.btn,a').forEach(el=>{el.addEventListener('pointerenter',()=>el.style.setProperty('--hover-scale','1.015'));el.addEventListener('pointerleave',()=>el.style.removeProperty('--hover-scale'))})})();
